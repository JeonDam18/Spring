<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.UserMapper">
	<select id="userLogin" parameterType="hashmap" resultType="com.example.test1.model.User">
		SELECT * 
		FROM TBL_USER
		WHERE USERID = #{userId} AND PWD = #{pwd} 
	</select>
	<select id="userList" parameterType="hashmap" resultType="com.example.test1.model.User">
		SELECT
			*
		FROM TBL_USER	
		WHERE 1=1
         <if test="category == 'cate_id'">
          AND USERID LIKE '%' ||  #{searchData} || '%'
        </if>
        <if test="category == 'cate_email'">
          AND EMAIL LIKE '%' ||  #{searchData} || '%'
        </if>
        <if test="category == ''">
          AND (USERID LIKE '%' ||  #{searchData} || '%'
          OR EMAIL LIKE '%' ||  #{searchData} || '%')
        </if> 
	</select>
	
	<select id="UserInfo" parameterType="hashmap" resultType="com.example.test1.model.User">
		SELECT
		*
		FROM TBL_USER
		WHERE USERID = #{userId}
	</select>
	
	<insert id="userJoin" parameterType="hashmap">
		INSERT INTO TBL_USER
		VALUES(#{userId},#{pwd},#{name},#{email},#{phone},#{gender},#{status},#{addr})
	</insert>
</mapper> 